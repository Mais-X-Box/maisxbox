import 'i_memory_data.dart';

class MemoryData implements IMemoryData {
  //AdventureEntity? adventure;

  void resetData() {
    //this.adventure = null;
  }

  Future loadLocalUser() async {
    // User returnedUser = (await UserRepository().load())?.first;
    // if (returnedUser != null) loggedUser = returnedUser;
  }
}
